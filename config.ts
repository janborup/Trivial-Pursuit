
export const APP_CONFIG = {
  // Base path for the application.
  // If hosting at https://your-site.com/trivial, set this to '/trivial'
  // If hosting at root, leave this as '' or '/'
  basePath: '/trivial',
};
